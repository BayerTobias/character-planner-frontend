import { ClassTranslatePipe } from './class-translate.pipe';

describe('ClassTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ClassTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
